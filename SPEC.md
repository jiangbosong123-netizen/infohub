# 行业情报站 · 系统设计说明书（SPEC）

> 主题与持久事件已于 2026-09-12 实现：当前数据模型、归并规则、验证和部署说明见 [AIHOT 对标说明](docs/AIHOT_ALIGNMENT.md)。下文旧 clusters 全量重建方案已被替代。

> 2026-09-12 评估后的变更与已知偏差见 [项目评估](docs/PROJECT_REVIEW.md)。以下设计基线中的旧规则以该评估和代码为准。
>
> 对标 aihot.news 的 TMT 行业信息聚合站。本文档描述系统当前的真实实现，
> 以及每个模块后续可做的改进方向（文末路线图）。

---

## 1. 系统总览

```
┌─────────────────────────── 数据流水线（每 5 分钟一拍）───────────────────────────┐
│                                                                                │
│  ①抓取层          ②清洗入库层        ③AI策展层          ④计算层         ⑤展示层  │
│  ┌──────────┐    ┌──────────┐      ┌──────────┐      ┌──────────┐  ┌────────┐ │
│  │38 个信息源│ →  │ URL归一化 │ →   │ GLM 翻译  │ →   │ 热度算法  │ → │FastAPI │ │
│  │6 种抓取器 │    │ 去重/钳制 │      │ 评分/过滤 │      │ 事件聚类  │  │Jinja2  │ │
│  └──────────┘    └──────────┘      │ 中文标题  │      └──────────┘  │ 7个页面│ │
│       ↑                            │ 推荐理由  │                    └────────┘ │
│  ⑥每日对账 06:30                   └──────────┘                                │
│  Google News 按公司全网核对，漏的补录并标记                                                  │
└────────────────────────────────────────────────────────────────────────────────┘
        ↓ 全程落库 SQLite(WAL)，launchd 托管常驻，崩溃自愈/开机自启
```

核心设计原则：**多层冗余防漏、单源故障不扩散、AI 可降级、全量可追溯**。

---

## 2. 抓取层

### 2.1 信息源注册表（`app/crawler/sources.py`）

所有源集中声明，一份注册表同时驱动「数据库注册」「调度」「源状态页」。

| 层级 tier | 源 | 频率 | 作用 |
|---|---|---|---|
| official 官方一手 | SEC EDGAR、港交所披露易、OpenAI Newsroom | 10-60min | 官方披露优先覆盖 |
| media 财经媒体 | 财联社电报、华尔街见闻快讯、新浪 7x24、CNBC、Techmeme、每公司 Google News 源 | 10-30min | 速度与广度 |
| info 科技资讯 | 量子位、IT之家、爱范儿、HN、TechCrunch、The Verge、HF Blog、IEEE、Robot Report | 30-60min | AI/机器人纵深 |
| reconcile 对账 | Google News 按公司检索 | 每日 06:30 | 防漏保险 |

### 2.2 六种抓取器（`FETCHERS` 注册表分发）

| type | 实现 | 备注 |
|---|---|---|
| rss | feedparser | 空feed+解析异常视为失败（防限流空页静默成功） |
| sec | SEC submissions JSON | CIK 缺失自动从官方映射表解析回填；限速 0.2s/req |
| hkex | 披露易两段式 | prefix.do 解析 stockId（缓存回填）→ titleSearchServlet 按日期窗拉公告；result 字段二次 JSON 解码 |
| sina | 新浪 7x24 JSON API | 分钟级中文快讯 |
| cls | 财联社 `/api/cache` | 签名算法来自 RSSHub：sorted(params)→SHA1→MD5 |
| wscn_live | 华尔街见闻 lives API | 端点来自 newsnow 项目 |
| googlenews | Google News RSS 按公司查询 | 每公司一源，别名严格匹配防漂移 |

### 2.3 调度与稳定性（`runner.py`）

- 每 5 分钟 tick，按「上次运行时间 + 间隔」挑出到期的源，8 线程并发
- **同域错峰**：同域名请求间隔 3 秒（防 Yahoo 式 429），跨域 0.3 秒
- **源级隔离**：单源失败只累计 `fail_count` 并记录错误，不影响其他源；成功即清零
- **子任务容错**：SEC/港交所按公司循环，单家公司失败记 `_error`，其他公司照常
- **睡眠恢复**：APScheduler `misfire_grace_time=3600`，Mac 睡醒后自动补跑错过的任务
- `fetch_log` 记录每次抓取（14 天自动清理），支撑源状态页

---

## 3. 清洗入库层

1. **URL 归一化去重**：小写 host、剥 `utm_*` 参数后作为唯一键
2. **未来时间钳制**：pubDate 标错的条目（如 OpenAI feed 曾标到 3 天后）钳到当前时间，防霸榜
3. **公司匹配**（`company_match.py`）：watchlist 别名匹配，ASCII 词边界、中文子串；命中写入 `item_companies` 多对多
4. **个股源过滤**：Google News 公司源要求标题命中绑定公司别名，防相关性漂移
5. **港交所降噪**：例行月报表等按标题规则跳过；公告分类（回购/业绩/并购/人事）

---

## 4. AI 策展层（`app/ai/pipeline.py`，GLM-4.6）

### 4.1 主流水线（每 15 分钟，清空式）

对未评分条目批量（60/批）输出 **7 个字段**：

| 字段 | 说明 |
|---|---|
| title_zh | 中文标题（英文翻译/中文润色） |
| tmt | 是否 TMT 相关 → **false 整站隐藏**（官方文件与公司事件例外保留） |
| score | 0-100 重要性（重大 80-100 / 一般 50-79 / 例行 <50） |
| summary_zh | 1-2 句中文摘要 |
| reason | 一句话推荐理由（为什么值得看） |
| event_type | 股市事件分类：财报/内部人/回购/并购/人事/产品/监管/评级/发行 |
| ai_cat | AI 子分类：模型/产品/行业/论文/观点 |

- 关闭深度思考（`thinking: disabled`）提速省钱；temperature 0.2
- **内容过滤降级**：整批触发智谱 1301 时自动改逐条处理，被过滤条目跳过不阻塞
- 处理节奏为**清空式**：每轮把积压清完才休息（上限 12 批），保证翻译/过滤追上抓取高峰

### 4.2 补处理任务

- `backfill_titles`：已评分但缺中文标题的条目补翻译
- `backfill_tmt`：未判定条目补 TMT 判定（官方/公司条目强制保留）

### 4.3 降级链

无 LLM Key → 纯聚合模式（无标题翻译/评分/过滤/理由，热度算法用默认基础分照常运行）。

---

## 5. 计算层（`app/ranking.py`）

### 5.1 条目热度

```
热度 = 基础分 × 时间衰减 × 官方加成
基础分 = AI评分/100（未评分时：例行 0.35 / 普通 0.5）
时间衰减 = 0.5^(小时数/18)    ← 半衰期 18 小时
官方加成 = 官方一手 ×1.25
```

### 5.2 热点聚类

- 窗口：最近 48 小时、仅 TMT 条目
- 算法：按时间倒序贪心——新条目与已有簇代表标题做相似度（子串包含=1.0，否则 SequenceMatcher ≥ 0.58）
- **股市频道加硬约束**：必须与簇内公司有交集才合并（防止不同公司的同名新闻误聚）
- 簇热度 = 最高条目热度 × (0.7 + 0.3×min(信源数, 6))
- 展示规则：热点榜优先多信源（≥2 家）事件，不足用单源高热度补位
- 每轮抓取后全量重建（当前 ~1400 簇），幂等

---

## 6. 展示层（FastAPI + Jinja2）

| 路由 | 内容 |
|---|---|
| `/?mode=selected` | **精选**（默认）：AI 高分 + 官方 + 公司事件，aihot 式热点卡 + 竖线时间轴 |
| `/?mode=all` | 全部动态：TMT 全量 |
| `/?channel=ai&cat=model…` | 频道 Tab（AI/机器人/股市）+ AI 子分类（模型/产品/行业/论文/观点） |
| `/?channel=stock&company=&event=` | 按公司 / 按事件类型筛选 |
| `/hot` | 热点榜：簇列表 + 每簇信源明细（锚点 `#cluster-N`） |
| `/daily`、`/daily/{date}` | 日报列表/详情（Markdown 渲染，GLM 生成带时间戳） |
| `/search?q=` | FTS5 trigram 全文搜索（≥3 字符），LIKE 兜底 |
| `/health` | 源状态：层级分组、最近成功、连续失败、错误信息 |

- **页面 2 分钟自动刷新** + 「数据更新于 X 分钟前」
- 暗色主题默认，可切浅色/跟随系统（localStorage 记忆）
- 移动端自适应（侧边栏折叠为顶部导航）
- 渲染时标题优先 `title_zh`，带「译」标记；评分 ≥70 条目时间轴圆点变绿

---

## 7. 每日任务

| 时间 | 任务 |
|---|---|
| 06:30 | **对账补漏**：对每家公司跑 Google News 全网检索，未入库的补录并标「对账补录」；同时统计常规源 24h 覆盖量 |
| 08:00 | **生成日报**：取前一日高分条目+热点簇，GLM 写 800 字三板块日报（总览/股市·AI·机器人/值得关注），无 Key 退化为结构化摘要 |
| 04:05 | 清理 14 天前的抓取日志 |

---

## 8. 数据模型（SQLite，WAL）

```
companies   公司清单（watchlist.yaml 驱动，CIK/stockId 自动解析回填）
sources     源注册表 + 健康状态（fail_count/last_success/last_error）
items       情报条目（title/title_zh/summary/score/tmt/reason/ai_cat/event_type/
            companies/official/via/heat…）
item_companies  条目↔公司多对多
clusters / cluster_members   热点簇
daily_reports   日报（按日期唯一，可覆盖重生成）
fetch_log       抓取日志（14 天）
items_fts       FTS5 trigram 全文索引（触发器同步）
```

## 9. 部署与运维

- macOS launchd：`KeepAlive`（崩溃自动拉起）+ `RunAtLoad`（开机自启）；项目位于 `~/infohub`（Documents 受 TCC 保护无法后台读取）
- 手动工具：`cli.py init-db / crawl / reconcile / ai / report / serve`
- 版本管理：GitHub 私有仓库 `jiangbosong123-netizen/infohub`
- Key 安全：`.env`（600 权限，gitignore），聊天中暴露过、建议轮换

---

## 10. 改进路线图（按模块）

### 抓取层
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★★ | 自建 RSSHub 实例 | 一条命令补齐机器之心/36氪等几十个失效源 |
| ★★★ | X/Twitter 账号监控 | aihot 的核心特色之一；可用 list API 或 RSSHub 路由 |
| ★★ | HTTP 缓存协商 | 记录 ETag/Last-Modified，304 直接跳过，省带宽更快 |
| ★★ | 源健康告警 | 连续失败时推送（邮件/飞书 webhook），不用手动看源状态页 |
| ★ | 真水位续抓 | RSS 目前靠全量拉取+去重；按 guid 水位可抗长睡眠窗口 |

### AI 策展层
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★★ | 事件影响方向 | 输出利好/利空/中性 + 受影响公司，股市频道直接显示 ▲▼ |
| ★★★ | 事件时间线 | 同一事件后续进展自动挂到簇下，形成「事件脉络」 |
| ★★ | 成本优化 | 簇内只精读代表条目，成员复用判定；预计省一半 token |
| ★★ | 本地模型兜底 | Ollama qwen3 作为 Key 限额/断网时的降级 |
| ★ | 摘要质量回归集 | 固定 50 条样本定期跑分，防止换模型后质量漂移 |

### 计算层
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★★ | 向量聚类 | embedding 相似度替代标题字面相似度，跨语言聚类（中英报道合并） |
| ★★ | 事件生命周期 | 簇状态：发展中/已定论/已冷，榜单只显示发展中 |
| ★ | 权重自适应 | 按点击/停留反馈调来源权重（需加访问统计） |

### 股市频道
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★★ | 实时行情 | 接免费行情 API（Yahoo/Stooq），条目旁显示当时股价与涨跌 |
| ★★ | 财报日历 | EDGAR 有公告时间表，提前标注「本周财报」 |
| ★★ | 公告 PDF 解析 | 港交所公告只存了标题；解析 PDF 可提炼回购金额等关键字段 |
| ★ | 财报速读 | 8-K/10-Q 正文摘要（收入/利润/指引三行） |

### 展示层
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★★ | 公司页 | 每家公司独立页：股价走势+时间线+官方文件+相关新闻聚合 |
| ★★ | 推送订阅 | 日报/高分事件推送到飞书或邮件，不用开网站等 |
| ★★ | 收藏与稍后读 | 本地书签功能 |
| ★ | 无限滚动 / 快捷键 | j/k 导航、b 收藏，类 HN 操作流 |
| ★ | Agent 接口 | 对标 aihot 的 /api/v1：对外提供只读 JSON API + MCP |

### 运维
| 优先级 | 改进 | 说明 |
|---|---|---|
| ★★ | 数据库每日备份 | SQLite backup API → iCloud/外部盘 |
| ★★ | 打通 GitHub Actions | CI 跑语法检查，防推坏代码 |
| ★ | 数据看板 | 每源抓取量/命中率趋势图 |

---

## 11. 已知边界

- 付费墙（Bloomberg 等）与 X 零散快讯未覆盖（后者接 API 后可解）
- Mac 睡眠期间无实时性（醒来按源窗口补抓，仍可能遗漏超出窗口的消息）
- 智谱内容过滤会拒判少量敏感条目（已降级处理，不影响整体）
